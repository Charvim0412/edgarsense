services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: edgarsense
      POSTGRES_PASSWORD: edgarsense
      POSTGRES_DB: edgarsense
    ports:
      - "5432:5432"
    volumes:
      - edgarsense_pgdata:/var/lib/postgresql/data

volumes:
  edgarsense_pgdata:
